name: Metrics
on:
  schedule: [{cron: "0 0 * * 1"}]
  push: {branches: ["master", "main"]}
jobs:
  github-metrics:
    runs-on: ubuntu-latest
    environment: 
      name: production
    permissions:
      contents: write
    steps:
      - uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          config_timezone: America/Sao_Paulo
          config_presets: https://raw.githubusercontent.com/mAngelo-dev/mAngelo-dev/refs/heads/main/preset/lunar_purple.yaml
          base: header, activity,languages
          plugin_steam_token: ${{ secrets.STEAM_WEB_API_TOKEN }}
          plugin_steam: no
          plugin_steam_user: ${{ secrets.STEAM_ID }}
          plugin_steam_sections: recently-played
          plugin_isocalendar: no
          plugin_languages: yes
          plugin_languages_ignored: html, css
          # plugin_music: yes
          # plugin_music_provider: youtube
          # plugin_music_playlist: https://music.youtube.com/playlist?list=PLOATcaWYVfKOKpN08bzi2L2rNL_B2NMj_

